# 結婚式Web招待状システム システム要件定義書 (REQUIREMENTS.md)

本ドキュメントは、GitHub Pages上で動作する結婚式Web招待状システムおよびGoogleスプレッドシート/Google Apps Script (GAS) バックエンド連携の全要件・仕様・データモデル・アーキテクチャを網羅した仕様定義書です。

---

## 1. システム概要 (System Overview)

本システムは、各招待客に個別のトークン付きURLを配布し、アクセス時にGoogleスプレッドシートから招待客のお名前やご家族情報を自動取得して画面描画するWeb招待状アプリケーションです。

- **ホスティング環境**: GitHub Pages (完全無料・サーバーレス)
- **データベース & バックエンド**: Googleスプレッドシート + Google Apps Script (GAS Web App API)
- **外部サービス連携**: zipcloud 郵便番号住所補完API (完全無料)

---

## 2. システム構成・アーキテクチャ

```text
[ 招待客 (ブラウザ/スマホ/PC) ]
         │
         │  ① URLアクセス (?token=SAMPLE_TOKEN)
         ▼
[ GitHub Pages (フロントエンド) ]
  ├── index.html (メイン画面 / RSVPフォーム / 確認モーダル)
  ├── css/style.css (結婚式向けエレガントデザイン)
  ├── js/config.js (GAS Web App URL / 式基本情報)
  └── js/app.js (通信制御 / JSONPフォールバック / 住所連動 / 動的UI)
         │
         │  ② GET (doGet / JSONP) : トークン照会・マスターデータ取得
         │  ③ POST (doPost) : フォーム送信・マスター＆履歴直接更新
         ▼
[ Google Apps Script (Code.gs) ]
         │
         ▼
[ Googleスプレッドシート (データベース) ]
  ├── Tokens シート (主ゲストマスターデータ)
  ├── PredefinedProxies シート (事前定義同伴者マスターデータ)
  └── Responses シート (回答履歴ログ)
```

---

## 3. スプレッドシート データモデル仕様 (3シート構成)

### 3.1 `Tokens` シート（主ゲストマスターテーブル）
招待状URLのトークンを発行する主ゲスト（本招待客）の基本情報および回答住所を直接保持します。

| 列 | 列名 (ヘッダー) | データ型 | 説明 | ユーザー送信時更新 | 例 |
|---|---|---|---|---|---|
| A | `Token` | String | ユニーク識別トークン（主キー） | - | `sample-guest-01` |
| B | `LastName` | String | 姓 (漢字) | ○ | `山田` |
| C | `FirstName` | String | 名 (漢字) | ○ | `太郎` |
| D | `KanaLastName` | String | せい (よみがな) | ○ | `やまだ` |
| E | `KanaFirstName` | String | めい (よみがな) | ○ | `たろう` |
| F | `Side` | String | 新郎側 / 新婦側 | ○ | `新郎側` |
| G | `AgeCategory` | String | 年齢区分 (大人 / 子供 / 幼児) | ○ | `大人` |
| H | `Email` | String | メールアドレス | ○ | `yamada@example.com` |
| I | `Status` | String | 回答状況 (未回答 / 出席 / 欠席) | ○ | `出席` |
| J | `PostalCode` | String | 郵便番号 | ○ | `1000001` |
| K | `Address` | String | 住所 (都道府県・市区町村・番地) | ○ | `東京都千代田区1-1-1` |
| L | `Building` | String | 建物名・部屋番号 (任意) | ○ | `千代田ビル 101` |
| M | `Phone` | String | 電話番号 | ○ | `09012345678` |

---

### 3.2 `PredefinedProxies` シート（事前定義代理出席者マスターテーブル）
管理者側で事前にトークンと紐づけて定義するご家族・同伴者様の専用テーブルです。1トークンに対して複数人登録可能です。

| 列 | 列名 (ヘッダー) | データ型 | 説明 | ユーザー送信時更新 | 例 |
|---|---|---|---|---|---|
| A | `ProxyId` | String | 代理出席者管理ID（主キー） | - | `proxy-sample-01` |
| B | `Token` | String | 紐づくTokensシートのToken (外部キー) | - | `sample-guest-01` |
| C | `LastName` | String | 姓 (漢字) | ○ | `山田` |
| D | `FirstName` | String | 名 (漢字) | ○ | `花子` |
| E | `KanaLastName` | String | せい (よみがな) | ○ | `やまだ` |
| F | `KanaFirstName` | String | めい (よみがな) | ○ | `はなこ` |
| G | `Side` | String | 新郎側 / 新婦側 | ○ | `新郎側` |
| H | `AgeCategory` | String | 年齢区分 (大人 / 子供 / 幼児) | ○ | `大人` |
| I | `Email` | String | メールアドレス (任意) | ○ | `hanako@example.com` |
| J | `Status` | String | 回答状況 (出席 / 欠席) | ○ | `出席` |
| K | `Allergies` | String | アレルギー・食事制限 | ○ | `えびアレルギー` |
| L | `SameAddress` | String | 主出席者と同じ住所 (はい / いいえ) | ○ | `はい` |
| M | `PostalCode` | String | 郵便番号 | ○ | `1000001` |
| N | `Address` | String | 住所 | ○ | `東京都千代田区1-1-1` |
| O | `Building` | String | 建物名・部屋番号 | ○ | `千代田ビル 101` |
| P | `Phone` | String | 電話番号 (任意) | ○ | `09087654321` |

---

### 3.3 `Responses` シート（回答履歴保存テーブル）
フォームから送信された全回答履歴ログを保存します。

| 列 | 列名 (ヘッダー) | 内容 |
|---|---|---|
| A | `Timestamp` | 送信日時 |
| B | `Token` | トークン |
| C | `Attendance` | 主出席者のご出欠 (出席 / 欠席) |
| D〜I | `LastName`〜`Email` | 主出席者の基本情報 |
| J〜N | `PostalCode`〜`Allergies` | 主出席者の住所・アレルギー |
| P | `PredefinedProxiesResponse` | 代理出席者全員の回答オブジェクト配列 (JSON) |
| Q | `AdditionalProxies` | 追加同伴者情報テキスト |
| R | `Message` | 新郎新婦へのお祝いメッセージ |

---

## 4. 機能要件 (Functional Requirements)

### 4.1 トークン自動認証 & 画面データ初期描画
1. URLのパラメータ `?token=XXX` からトークンを抽出。
2. GAS API (`doGet`) に問い合わせ、スプレッドシートから主ゲスト情報および代理出席者データを取得。
3. 取得した「お名前（漢字・かな）、新郎側/新婦側区分、年齢区分、住所、電話番号」をフォーム各項目に初期セット。
4. 再アクセス時は保存済みの最新回答内容（住所・アレルギー・メッセージ等）を自動復元表示。
5. 無効なトークンの場合は丁寧なエラー案内を表示。

### 4.2 出欠に応じたインタラクティブ表示制御
- **「ご出席」選択時**: 連絡先・住所入力欄、アレルギー入力欄、同伴者アコーディオンセクションを展開・表示。
- **「ご欠席」選択時**: 住所やアレルギー等の詳細欄を自動非表示化（お名前とメッセージ欄のみ表示）。

### 4.3 郵便番号住所自動入力機能
- 7桁の郵便番号を入力して「住所自動入力」ボタンを押すと、`zipcloud` APIから都道府県・市区町村・番地までの住所を取得して自動補完。

### 4.4 事前定義代理出席者（ご家族等）のアコーディオン式編集機能
- `PredefinedProxies` シートに登録された代理出席者ごとに、**出欠チェックボックス一体型アコーディオンカード**を生成。
- **ご出席チェック（初期表示）**:
  - アコーディオンが展開し、お名前（姓・名）、ふりがな（せい・めい）、年齢区分、アレルギー・食事制限を直接補正・編集可能。
  - お名前を入力するとヘッダータイトル（例: `山田 花子 様（大人）`）がリアルタイム同期。
- **ご欠席（未チェック時）**:
  - アコーディオンが折りたたまれ、該当同伴者は「ご欠席」として送信。

### 4.5 代理出席者の「主出席者と同じ住所」連動・個別入力機能
- 代理出席者カード内に **「☑ 主出席者（ご回答者様）と同じ住所」** チェックボックス（初期値: ON）を設置。
- **ONの時**: 主出席者の住所入力および郵便番号自動補完と連動して、同伴者の住所データもリアルタイムで自動同期。
- **OFFの時**: 代理出席者個別の住所入力ボックス（郵便番号、住所自動検索ボタン、住所、建物名、電話番号）が展開し、別住所を入力・保存可能。

### 4.6 送信前確認モーダル & 送信処理
- 「入力内容を確認する」ボタンで、主出席者・代理出席者の最新の編集内容（出欠、住所、アレルギー、メッセージ）を綺麗にまとめた確認モーダルを表示。
- 送信時、`Tokens` シートおよび `PredefinedProxies` シートの該当行セルをダイレクト上書き更新し、`Responses` シートに回答履歴を蓄積。

### 4.7 通信セキュリティ & CORS / JSONP 自動フォールバック
- ブラウザの `fetch()` 通信がCORSポリシーでブロックされた場合、自動的に **JSONP方式** へ切り替えて通信を完走させ、通信エラーを防止。

---

## 5. デザイン・非機能要件 (Non-Functional Requirements)

1. **デザインテーマ**:
   - パステルローズ、ローズゴールド、アイボリー調のカラーパレット。
   - 和風・エレガントな明朝体フォント (`Shippori Mincho`, `Noto Serif JP`) と視認性の高いゴシック体フォントの組み合わせ。
2. **モバイルファースト・レスポンシブ**:
   - スマートフォン（iOS / Android）でのタッチ操作に最適化されたボタン・アコーディオンサイズ。
3. **完全無料運用**:
   - サーバー維持費、データベース費用、住所検索API利用料がすべて永久無料（0円）。

---

## 6. 自動初期化・デプロイ手順

### 6.1 スプレッドシート自動初期化 (`createWeddingSheets`)
1. スプレッドシートの **「拡張機能」>「Apps Script」** を開く。
2. `gas/Code.gs` を貼り付けて保存。
3. 関数選択ドロップダウンで **`createWeddingSheets`** を選び「実行」を押す。
4. 全3シート（`Tokens`, `PredefinedProxies`, `Responses`）が自動生成・装飾完了。

### 6.2 GAS Web App デプロイ設定
1. 右上の **「デプロイ」>「新しいデプロイ」** をクリック。
2. 実行ユーザー: `自分` / アクセスできるユーザー: **`全員 (Anyone)`** に設定してデプロイ。
3. 発行された Web App URL を `js/config.js` に貼り付け。
